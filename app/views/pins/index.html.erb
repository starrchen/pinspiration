<h2>Pins <%= link_to "(+)", new_board_pin_path(current_user) %></h2>

<div class="pin-body">
  <% @pins.each do |pin| %>
    <div class="each-pin">
    <h3><%= pin.title %></h3>
    <%= image_tag "#{pin.image_url}", class: "pin-image" %>
    </div>
  <% end %>
</div>
